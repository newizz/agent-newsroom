[project]
name = "agent-newsroom-notebooklm"
version = "0.1.0"
description = "NotebookLM integration tools used by Rin (Deep Researcher) in agent-newsroom."
requires-python = ">=3.10"
dependencies = [
    "notebooklm-py[browser]>=0.5.0",
]

[tool.uv]
package = false
